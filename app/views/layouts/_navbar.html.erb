<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Inicio', songs_path, :class => "navbar-brand"  %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        
        <li><%= link_to "Mi Lista", user_user_songs_path(current_user.id) if current_user %></li>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Géneros <span class="caret"></span></a>
        
          <ul class="dropdown-menu">
            <% Genre.all.each do |genre| %>
            <li><a href="<%= songs_path(genre_id: genre.id) %>"><%= genre.name %></a></li>
            <% end %>
            <li role="separator" class="divider"></li>
            <li><a href="<%= songs_path %>">Todos</a></li>
          </ul>

        </li>

        <li><%= link_to "Usuarios", users_path %></li>

      </ul>

      <ul class="nav navbar-nav navbar-right">
        <!-- devise authentication -->
        <% if user_signed_in? %>
          <li><a href="#">Hola <%= current_user.name %></a></li>
          <li><a href="<%= destroy_user_session_path %>" data-method="delete">Salir</a></li>
        <% else %>
          <li><a href="<%= new_user_session_path %>">Ingresar</a></li>
          <li><a href="<%= new_user_registration_path %>">Registrarse</a></li>
        <% end %>

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>